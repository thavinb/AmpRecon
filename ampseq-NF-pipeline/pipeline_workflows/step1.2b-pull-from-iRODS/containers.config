process {
  withName:scramble_cram_to_bam{
      container = "$projectDir/../containers/staden.sif"
  }
}

// all remaining process used on 1.2b rely on iRODS software, so the pipeline
// assumes is all locally available on the machine. Therefore, no need for
// containers
